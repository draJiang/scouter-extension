export type PromptType = {
    title: string;
    getUnsplashImages: boolean;
    userPrompt: string;
    id: string;
};

// export type chatMessage = {
    // chatId: msg.chatId,
    // content: msg.content,
    // loading: false,
    // prompt: prompt[0]['content']
// };